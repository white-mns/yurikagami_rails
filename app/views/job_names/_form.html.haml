= form_with(model: job_name, local: true) do |form|
  - if job_name.errors.any?
    #error_explanation
      %h2
        = pluralize(job_name.errors.count, "error")
        prohibited this job_name from being saved:
      %ul
        - job_name.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :job_id
    = form.number_field :job_id, id: :job_name_job_id
  .field
    = form.label :name
    = form.text_field :name, id: :job_name_name
  .actions
    = form.submit
