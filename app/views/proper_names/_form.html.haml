= form_with(model: proper_name, local: true) do |form|
  - if proper_name.errors.any?
    #error_explanation
      %h2
        = pluralize(proper_name.errors.count, "error")
        prohibited this proper_name from being saved:
      %ul
        - proper_name.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :proper_id
    = form.number_field :proper_id, id: :proper_name_proper_id
  .field
    = form.label :name
    = form.text_field :name, id: :proper_name_name
  .actions
    = form.submit
